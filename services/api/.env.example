SECRET_KEY=dev-brasaland-change-me-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60
ALGORITHM=HS256

# Password reset (AUTH-03)
RESET_TOKEN_EXPIRE_MINUTES=30
# URL del backoffice (enlace del email → /reset-password?token=...)
FRONTEND_URL=http://localhost:3000

# Resend — https://resend.com (API key NUNCA en el código fuente)
# Sin RESEND_API_KEY el enlace se imprime en la consola de uvicorn (demo local).
RESEND_API_KEY=
RESEND_FROM_EMAIL=Brasaland OPS <onboarding@resend.dev>

# Inventario (SQLModel) — Supabase Postgres (Transaction pooler URI)
# Dashboard → Project Settings → Database → Connect → Transaction pooler
# Si está vacío, la API usa SQLite local en data/inventory.db (solo demo/dev).
DATABASE_URL=
