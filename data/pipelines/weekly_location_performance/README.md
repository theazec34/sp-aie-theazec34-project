# Placeholder for the Prefect flow package (implementation hito).
#
# Planned public functions (imported by services/reporting — never the reverse):
# - query_weekly_location_performance(week_start) -> dict
# - get_latest_run(pipeline_name) -> dict
# - trigger_weekly_location_performance_flow(week_start, triggered_by) -> dict
#
# See PIPELINE_DESIGN.md (Fases 4–5).
